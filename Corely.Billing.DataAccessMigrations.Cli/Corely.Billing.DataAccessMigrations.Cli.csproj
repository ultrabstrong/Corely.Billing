<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>

    <PackAsTool>true</PackAsTool>
    <ToolCommandName>corely-billing-db</ToolCommandName>
    <PackageId>Corely.Billing.DataAccessMigrations.Cli</PackageId>
    <Title>Corely Billing Migration CLI</Title>
    <Description>Command-line tool that creates and migrates the Corely Billing database schema. Ships the SQL Server and MySQL migrations, so a consumer of the Corely.Billing package can stand up a database without referencing anything beyond this tool.</Description>
    <PackageTags>Corely Billing Migrations EntityFrameworkCore Database CLI DotnetTool</PackageTags>
    <RepositoryUrl>https://github.com/ultrabstrong/Corely.Billing</RepositoryUrl>
    <PackageProjectUrl>https://github.com/ultrabstrong/Corely.Billing</PackageProjectUrl>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <Version>1.0.0</Version>

    <!-- Single-file publishing options. Applied only when publishing: a self-contained
         executable cannot be referenced by a non-self-contained one, which would block a test
         project from referencing this. RebuildAndTest.ps1 passes the publish-time equivalents.
         PackAsTool is unaffected - `dotnet pack` does not set _IsPublishing. -->
    <PublishSingleFile Condition="'$(_IsPublishing)' == 'true'">true</PublishSingleFile>
    <SelfContained Condition="'$(_IsPublishing)' == 'true'">true</SelfContained>
    <EnableCompressionInSingleFile>true</EnableCompressionInSingleFile>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>

    <!--
    Trimming is disabled because EF Core migrations rely heavily on reflection
    and are not compatible with IL trimming. This results in a larger executable
    but ensures migrations work correctly at runtime.
    -->
    <PublishTrimmed>false</PublishTrimmed>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Corely.Billing.DataAccessMigrations.Cli.UnitTests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Corely.Common" Version="2.0.3" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="10.0.11" />
    <PackageReference Include="System.CommandLine" Version="2.0.11" />
  </ItemGroup>

  <ItemGroup>
    <None Include="README.md">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
    <None Include="..\LICENSE">
      <Pack>True</Pack>
      <PackagePath>\</PackagePath>
    </None>
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Corely.Billing.DataAccessMigrations.MySql\Corely.Billing.DataAccessMigrations.MySql.csproj" />
    <ProjectReference Include="..\Corely.Billing.DataAccessMigrations.MsSql\Corely.Billing.DataAccessMigrations.MsSql.csproj" />
  </ItemGroup>
</Project>
